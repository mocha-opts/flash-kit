{
  "name": "@repo/billing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    "./server": "./src/server.ts",
    "./config": "./src/config/index.ts",
    "./types": "./src/types.ts",
    "./components": "./src/components.ts"
  },
  "imports": {
    "#billing-config/*": "./src/config/*.ts",
    "#*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@better-auth/core": "catalog:",
    "@better-auth/stripe": "catalog:",
    "@repo/config": "workspace:*",
    "@repo/db": "workspace:*",
    "react": "catalog:",
    "server-only": "catalog:",
    "stripe": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@repo/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "typescript": "catalog:"
  }
}
